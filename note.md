gulp-plumber

path.json

on dom start

!for loop don't need len varible

!don't use delete, use slice

!remove copy of player

!don't render in collision, move bulletArray render upwards

!bablify

hot-reloading browser-sync alternative for js

npm script to start

!call canvas.getSize(); once and reuse

pytha store (pos1.x - pos2.y) in var

!don't need else in limit1Vel

!don't need brackets in limitVel

!don't need to have a return true & return false

ES6:

getter & setter
